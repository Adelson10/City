on: 
    push:
        branches:
            - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:

    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: Use Node.js 16
      uses: actions/setup-node@v2
      with:
        node-version: '20'
      
    - name: 🔨 Build Project
      run: |
        cd front-end
        npm install
        npm run build
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: adelsonbarros.com
        username: id_adelson
        password: fn&I6f!*Z*0W
        port: 22
        protocol: ftp
        server-dir: city.adelsonbarros.com/
